export enum STAGE_SETTINGS_ENUMS {
    WIDTH = 1080,
    HEIGHT = 1920
}

export enum PLAYING_AREA_ENUMS {
    WIDTH = 1080,
    HEIGHT = 1920,
    X = 0,
    Y = 0,
    COLUMNS = 3,
    ROWS =  12
}

export enum GENERAL_GAMEPLAY {
    DROP_ROW_MS = 300,
    POINTS_PER_LINE = 10,
}